import 'package:flutter/material.dart';

class QuizzService extends StatelessWidget {
  const QuizzService({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
